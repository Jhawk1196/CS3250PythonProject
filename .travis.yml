language: python
python:
  - "3.6"
  - "3.7"

dist: trusty

addons:
  sonarcloud:
    organization: "jhawk1196"
    token: ${SONAR}

before_install:
  - pip install coverage

script:
  - coverage run -m unittest
  - coverage report
  - sonar-scanner

after_success:
  - coveralls